<%= simple_form_for @workday_product do |f| %>
<%= f.association :product, type: :select, disabled: @workday_product.persisted? %>
<%= f.input :stock, type: :integer %>
<%= f.input :deliver_time, as: :time, html5: true %>
<%= f.hidden_field :workday_id %>

<%= f.submit type: :submit, class: "px-4 py-2 rounded-md bg-yellow-500 text-white cursor-pointer" %>
<% end %>